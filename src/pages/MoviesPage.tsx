import React from 'react';
import {NavLink, Outlet} from "react-router-dom";

const MoviesPage = () => {
    return (
        <div>
            list of movies
        </div>
    );
};

export default MoviesPage;
